{
    "name": "@tootega/dase-mcp",
    "version": "2.0.666",
    "description": "Standalone MCP server for the DASE ORM Designer - connects to the loopback agent bridge exposed by the DASE VS Code extension and translates MCP tool calls into bridge calls. Publishable as a Claude Code plugin.",
    "author": "Hermes Silva <hxpelo@gmail.com>",
    "license": "MIT",
    "main": "dist/server/index.js",
    "types": "dist/server/index.d.ts",
    "bin": {
        "dase-mcp": "claude-plugin/server/dase-mcp.cjs"
    },
    "files": [
        "dist",
        "claude-plugin",
        "README.md"
    ],
    "scripts": {
        "build": "tsc -p tsconfig.json && npm run bundle",
        "bundle": "esbuild src/server/index.ts --bundle --platform=node --format=cjs --outfile=claude-plugin/server/dase-mcp.cjs",
        "typecheck": "tsc -p tsconfig.json --noEmit"
    },
    "dependencies": {
        "@modelcontextprotocol/sdk": "^1.29.0",
        "zod": "^3.25.76"
    },
    "devDependencies": {
        "@types/node": "^20.14.0",
        "esbuild": "^0.24.0",
        "typescript": "^5.5.0"
    },
    "engines": {
        "node": ">=18"
    }
}
